==============================================================
TICKET
==============================================================
ID:            T-YYYYMMDD-#########   (9-digit random number)
CREATION:      New tickets MUST be created through `python lib/ticket_writer.py ...`;
               never copy this file or choose/count an ID manually.
TITLE:         <short problem title>
CREATED:       YYYY-MM-DD
STATUS:        <CLUSTER>[/<subcategory>] (since YYYY-MM-DD) — categories v1:
               INBOX | ACTIONABLE | QUEUED | BLOCKED/host-receipt|foreign-state|
               lock|quota|dependency | WAITING/scheduled|review-due|marker |
               USER/decision|data|freigabe|hardware|session|marker
               (USER/marker = user supplies/confirms marker) |
               PARKED/skip|backlog|until-trigger | SOLVED
               (legacy aliases: OPEN | PENDING | .USER — see docs/CATEGORIES.en.md)
PRIORITY:      low | medium | high | critical

--------------------------------------------------------------
PROJECT ASSIGNMENT
--------------------------------------------------------------
PIPELINE:      <e.g. software / research / docs / infra / ...>
PROJECT_DIR:   <relative or absolute path, or project name>
CONTROL_FILE:  <CLAUDE.md / README.md / START.md that confirmed GATE1>
DOMAIN:        <domain id from config/domains.json, or n/a — see URGENCY GATE>
ENDPOINT:      <standalone skill/module(s) the domain resolved to (a single
               skill for an exact match, or a comma-separated list for a
               fuzzy "teilportiert" skill family — see matched_skills in
               domains.json); n/a if no domain match; or "GAP — no
               standalone skill yet (<expert>)" if the domain matched but no
               standalone skill exists yet>
URGENCY:       <sofort | heute | woche | backlog — see config/urgency.json>

--------------------------------------------------------------
ROUTING CONTRACT v2  (omit this block for legacy schema-v1 tickets)
--------------------------------------------------------------
ROUTING_SCHEMA: 2
CREATE_IDEMPOTENCY_KEY:<stable caller key; optional for humans, required for retry-safe integrations>
CREATE_REQUEST_FINGERPRINT:<sha256 of the normalized creation request>
TICKET_KIND:   <normal | transfer | fork>
TARGET_KIND:   <any | all | grouped | exact>
TARGET_SYSTEMS:<JSON snapshot of canonical system IDs; never invent hosts>
TARGET_SYSTEM_DETAILS:<JSON public slot/host/capability projection>
TARGET_SNAPSHOT_AT: <absolute UTC contract-creation timestamp>
TARGET_REGISTRY_CHECKED_AT:<absolute UTC source-check timestamp>
TARGET_SNAPSHOT_SOURCE: <canonical inventory source>
TARGET_SNAPSHOT_FINGERPRINT: <sha256 evidence>
PRIMARY_TICKET:<ticket ID>
ORIGINAL_OWNER:<origin system/actor; not automatically a completed target>
RECEIPT_TO:   <ticket ID or canonical receipt reference>
EXECUTOR:     <any | registered runner | mixed>
MODEL_SELECTION: <self | family | exact | mixed>
MODEL_SELECTOR: <canonical Clutch selector; empty when unbound>
BINDING_MODE: <required | preferred>
BINDING_EXPIRES_AT: <absolute UTC timestamp | never>
BINDING_STATE:<unbound | active | unresolved | expired-unbound>
RESOLUTION_NOTE:<JSON from the public Clutch resolver, including fingerprint/time>
EXECUTION_MATRIX:<JSON object; required with via-mixed>
CLAIMED_BY_HOST:<temporary lease host; never infer from target>
CLAIMED_BY_ACTOR:<actual lease actor>
CLAIMED_AT:   <absolute UTC timestamp>
CLAIM_LEASE_UNTIL:<absolute UTC timestamp>
SYSTEM_LEDGER:<JSON rows, exactly one per target: pending|claimed|done|blocked>

Canonical v2 filename:
`T-ID[.to-<target>][.via-<Clutch selector>][.claim-<HOST>].txt`.
The axes are independent and ordered. Existing `T-ID.<HOST>.txt` files remain
legacy claims. Create v2 contracts through `create_routed_ticket()` or the
`ticket_writer.py` CLI; never copy this template or mint an ID manually.

--------------------------------------------------------------
PROBLEM DESCRIPTION
--------------------------------------------------------------
<What is the problem or change request?
 Observed behaviour, expected behaviour, reproduction steps,
 affected files — as much detail as needed.>

--------------------------------------------------------------
TASK CHARACTERISTICS  (Processing chain steps 2–3)
--------------------------------------------------------------
TYPE:          <Bug / Feature / Docs / Research / Review / Refactor>
REQUIREMENTS:  <Clarity, Complexity, Creativity, Context, Criticality>
SCORE:         <0–50 per score formula in TICKET-MASTER.md section (c)>

--------------------------------------------------------------
MODEL ROUTING  (Processing chain steps 4–5)
--------------------------------------------------------------
CANDIDATE 1:   <model + invocation>     [LLM-launchable: yes/no]
CANDIDATE 2:   <model + invocation>     [LLM-launchable: yes/no]
CANDIDATE 3:   <model + invocation>     [LLM-launchable: yes/no]
CHOSEN:        <candidate + rationale>

--------------------------------------------------------------
TASK PROMPT  (for sub-agent; includes routing + root docs to read)
--------------------------------------------------------------
<Concrete work order that can be passed as a prompt.
 MUST include project routing + instruction to read the relevant
 pipeline root documents before starting.>

--------------------------------------------------------------
HISTORY / LOG
--------------------------------------------------------------
YYYY-MM-DD  Created.
YYYY-MM-DD  ...

--------------------------------------------------------------
SOLUTION / RESULT  (fill before moving to SOLVED/)
--------------------------------------------------------------
<What was done, by whom / which model, empirical confirmation.>
==============================================================
